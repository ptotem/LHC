<%= content_for(:dialogue_bar_header) do %>
    Mutual likes
<% end %>
<%= content_for(:page_header) do %>
    Here are your filtered matches!
<% end %>

<div class="row" style="margin-top: 5%;">
  <div class="col-xs-11 col-sm-11 col-md-11 col-lg-11 col-xs-offset-1 col-sm-offset-1 col-md-offset-1 col-lg-offset-1">
    <% if @mutual_likes.length > 0 %>
        <% @mutual_likes.each do |m| %>
            <div class="col-xs-6 col-sm-6 col-md-6 col-lg-6" style="margin-bottom: 5%;">
              <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12" style="width: 100%; margin-left: 0; margin-top: 0; background: none repeat scroll 0 0 #ffffff;">
                <a href="<%= user_profile_path(m.id) %>">
                  <img src="<%= get_user_image(m.id) %>" class="img-responsive" alt="Image Size 430x280" style="display:table-cell; width: 100%;">
                </a>
                <span class="title" style="display: inline-block; text-align: center; width: 100%;">
                    <span>
                      <%= get_user_name(m.id) %>
                      <br>
                      <%= get_user_age(m.id) %>,<%= get_user_gender(m.id) %>
                      <br>
                      <%= get_user_location(m.id) %>
                    </span>
                </span>
                <div class="row">
                  <div class="col-xs-12 col-sm-12 col-md-12 col-lg-12">
                    <% if check_icebreaker_status(m.id) %>
                        <button class="send-message_btn" id="send_message_btn" onclick="window.location='<%= conversations_with_users_path(m.id) %>'">Send
                          Message
                        </button>
                    <% else %>
                        <button class="send-icebreaker_btn" id="send_icebreaker_btn" onclick="window.location='<%= start_ice_breaker_path(m.id) %>'">Send
                          Icebreaker
                        </button>
                    <% end %>
                  </div>
                </div>
              </div>
            </div>
        <% end %>
    <% else %>
        <div style="font-size: 20px">
        <h4 class="white" style="font-size: 1em; font-weight: bold; line-height: 1.2em;"> “Bummer – no mutual likes yet it seems :(" </h4>

          <p class="white" style="margin-top: 5%">But don’t worry soldier, you gotta keep trying!!! Never give up we say! Here’s what
            you can do to increase your chances of getting a mutual like:</p>
          <ol class="white" style="margin-top: 5%">
            <li><%= link_to "Complete your profile", user_profile_path(current_user.id) %> –Seriously, if it’s not
              complete yet, complete it. Really. Like right now.
            </li>
            <br>
            <li><%= link_to "Snaz it up! ", snazzmeup_path %>– Basically, make it impossible for a match to look away from
              your profile. Take our quizzes found under Snaz it up! to show off your interests and to show how you’ll be
              so much fun to talk to.
            </li>
            <br>
            <li><%= link_to "Get a drink", "https://www.google.co.in/search?q=bars+i%3Dnear+me&oq=bars+i%3Dnear+me&espv=2&es_sm=117&ie=UTF-8#q=bars+near+me", :target => "_blank" %>
              – Some things are just not in your control. Relax, get a drink, and chill. Patience is key.
            </li>

          </ol>
        </div>
    <% end %>
  </div>
</div>