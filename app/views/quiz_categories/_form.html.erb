<%= form_for(@quiz_category) do |f| %>
  <% if @quiz_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@quiz_category.errors.count, "error") %> prohibited this quiz_category from being saved:</h2>

      <ul>
      <% @quiz_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :intro %><br>
    <%= f.text_area :intro %>
  </div>
  <div class="field">
    <%= f.label :status %><br>
    <%= f.check_box :status %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
